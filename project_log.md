# Pneumonia Detection Project Log

This document tracks all edits, iterations, and decisions made during the development of the Pneumonia Detection AI model.

## Iteration 1: Project Setup and Notebook Generation
**Date**: 2026-05-05
**Action**: 
- Initialized the project structure in `d:\Code\Pnemonia Detection`.
- Confirmed dataset (`xray_dataset_covid19`) is present with `train` and `test` splits containing `NORMAL` and `PNEUMONIA` categories.
- Decided to build the entire pipeline in a Jupyter Notebook (`pneumonia_detection.ipynb`) as per user request.
- Drafted a Python script to generate the initial notebook structure including PyTorch data loading, simple CNN, ResNet18 transfer learning, resumable training loop with checkpointing, Grad-CAM visualization, and a Gradio web app interface.

**Next Steps**:
- Run the generation script to create the `.ipynb` file.
- Verify the notebook contents.

## Iteration 2: Architecture Split (Notebook & Separate Web Demo)
**Date**: 2026-05-05
**Action**:
- Based on user feedback, separated the modeling logic from the deployment logic.
- Updated `create_notebook.py` to remove the Gradio web application section and regenerated `pneumonia_detection.ipynb` to strictly handle training, checkpointing, and evaluation.
- Created `app.py` as a dedicated web deployment file containing the Gradio interface. It handles loading the `resnet_checkpoint.pth` (or CNN checkpoint) generated by the notebook, running inference, and calculating the Grad-CAM heatmaps.
- This modular structure ensures the `.ipynb` stays lightweight for training and `app.py` is immediately ready for deployment to hugging face spaces.
